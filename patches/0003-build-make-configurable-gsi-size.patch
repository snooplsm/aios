diff --git a/target/board/BoardConfigGsiCommon.mk b/target/board/BoardConfigGsiCommon.mk
index 8a62796e51..d25cc57c63 100644
--- a/target/board/BoardConfigGsiCommon.mk
+++ b/target/board/BoardConfigGsiCommon.mk
@@ -65,10 +65,10 @@ BOARD_AVB_SYSTEM_ADD_HASHTREE_FOOTER_ARGS += --hash_algorithm sha256
 
 ifdef BUILDING_GSI
 # super.img spec for GSI targets
-BOARD_SUPER_PARTITION_SIZE := 3229614080
+BOARD_SUPER_PARTITION_SIZE ?= 3229614080
 BOARD_SUPER_PARTITION_GROUPS := gsi_dynamic_partitions
 BOARD_GSI_DYNAMIC_PARTITIONS_PARTITION_LIST := system
-BOARD_GSI_DYNAMIC_PARTITIONS_SIZE := 3221225472
+BOARD_GSI_DYNAMIC_PARTITIONS_SIZE ?= 3221225472
 
 # Build pvmfw with GSI: b/376363989
 ifeq (true,$(PRODUCT_BUILD_PVMFW_IMAGE))
diff --git a/target/product/gsi/Android.bp b/target/product/gsi/Android.bp
index 00bc7fefbf..c8512cc955 100644
--- a/target/product/gsi/Android.bp
+++ b/target/product/gsi/Android.bp
@@ -186,6 +186,29 @@ android_system_image {
     }),
 }
-
+// Additive products must enumerate their extra Kati PRODUCT_PACKAGES in the
+// selected Soong filesystem. Keep the wrapper beside android_gsi_defaults so
+// its private build.prop dependencies retain AOSP's namespace boundary. The
+// stable product-policy dependency carries AIOS modules through transitive
+// required-module packaging specs.
+android_system_image {
+    name: "aios_gsi_system_image",
+    defaults: ["android_gsi_defaults"],
+    deps: [
+        "aios_product_policy",
+        "system_ext_userdebug_plat_sepolicy.cil",
+    ] + select(soong_config_variable("gsi", "import_usb_debugging_test_app"), {
+        true: ["UsbDisableDebugger"],
+        default: [],
+    }),
+    enabled: select(soong_config_variable(
+        "ANDROID",
+        "PRODUCT_INSTALL_DEBUG_POLICY_TO_SYSTEM_EXT",
+    ), {
+        "true": true,
+        default: false,
+    }),
+}
+
 // system.img for aosp_{arch} targets
 android_system_image {
     name: "aosp_system_image",
