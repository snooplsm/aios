# Gemma 4 E2B model-pack verification

This record was captured from an external, generated Pixel 9a candidate model
pack at AIOS revision `522284897fafc9098ba2b612b53e334e5c1007ed`.

The pack verifier rehashed the catalog-pinned 2,588,147,712-byte LiteRT-LM
artifact and both packaged Apache-2.0 license copies. The text and multimodal
catalog roles resolve to one physical model payload. Model weights, external
paths, and the private builder-acceptance record are not stored here.

This is packaging evidence only. It does not prove model initialization,
inference latency, call-path behavior, or operation on a physical Pixel 9a;
those remain separate runtime gates.
