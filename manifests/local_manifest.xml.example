<?xml version="1.0" encoding="UTF-8"?>
<manifest>
  <!-- Replace this deliberately invalid URL after AIOS has a canonical remote. -->
  <remote name="aios" fetch="https://example.invalid/replace-with-aios-owner" />
  <project
      name="aios"
      path="vendor/aios"
      remote="aios"
      revision="main" />
</manifest>
